import { LoadingBarStyleScheme } from "@/partials/ui/components/LoadingBar";

export const BOOT_LAYOUT = {
    titleOffsetY: -40,
    loadingBarOffsetY: 40,
    loadingBarStyle: {
        width: 640,
        height: 18,
        paddings: 4,
    } as LoadingBarStyleScheme,
};